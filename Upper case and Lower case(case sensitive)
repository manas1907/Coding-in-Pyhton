input_string = "Eleven benevolent elephants"
frequencies = {}
for char in input_string:
	if char in frequencies:
		frequencies[char] += 1
	else:
		frequencies[char] = 1
print (" frequency in '{}' is :\n {}".format(input_string, str(frequencies)))
